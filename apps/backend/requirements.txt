# JetApi Requirements
# Production-grade FastAPI stack

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=22.0.0

# Database (Async)
sqlalchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0
aiosqlite>=0.20.0

# Redis
redis[hiredis]>=5.0.0

# Validation & Settings
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0

# Security
bcrypt==4.0.1
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.17

# Job Queue
arq>=0.25.0

# Environment
python-dotenv>=1.0.1

# HTTP Client (for OAuth2 JWKS fetching + external services)
httpx>=0.28.0

# Resilience
tenacity>=9.0.0
pybreaker>=1.2.0

# CLI & UI
rich>=13.9.0

# Email - AWS SES (optional, for SES API support)
boto3>=1.35.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Database Drivers
psycopg2-binary>=2.9.10  # Sync PostgreSQL (for Alembic and general SQL execution)
# mysql-connector-python>=9.1.0  # Sync MySQL
aiomysql>=0.2.0  # Async MySQL (remove if not using MySQL)

# Supabase Admin SDK (async klijent za Auth admin operacije — brisanje korisnika)
supabase>=2.0.0
